/** UI-facing slices of the settings model, kept apart so the HUD never reaches into the engine. */
export { PRESETS } from '../core/settings.js'

/** Display order for the world picker: brightest and friendliest first. */
export const PLANETS_ORDER = ['sakura', 'festival', 'skyward', 'spirit', 'neon', 'winter']
